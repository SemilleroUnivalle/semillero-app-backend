server {
    listen 80;
    server_name semillero-dev.duckdns.org;

    # Necesario para que Certbot valide el dominio
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Temporalmente servimos la app por el puerto 80 para poder generar el certificado
    location / {
        proxy_pass http://backend:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
        alias /app/staticfiles/;
    }

    location /media/ {
        alias /app/media/;
    }
}

# COMENTADO TEMPORALMENTE: Habilita esto después de generar los certificados con Certbot
# server {
#     listen 443 ssl;
#     server_name semillero-dev.duckdns.org;
#
#     ssl_certificate /etc/letsencrypt/live/semillero-dev.duckdns.org/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/semillero-dev.duckdns.org/privkey.pem;
#
#     client_max_body_size 20M;
#
#     location / {
#         proxy_pass http://backend:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /static/ {
#         alias /app/staticfiles/;
#     }
#
#     location /media/ {
#         alias /app/media/;
#     }
# }